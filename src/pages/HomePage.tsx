import DashboardPage from './DashboardPage';

/**
 * HomePage renders the ATS Dashboard.
 * The dashboard provides a full status overview of the recruitment pipeline
 * with responsive widgets for different screen sizes.
 */
function HomePage() {
  return <DashboardPage />;
}

export default HomePage;
